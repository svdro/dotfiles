# tmux

### dependencies (_macOS_)

reattach-to-user-namespace _(clipboard management for macOS)_
```bash
brew install reattach-to-user-namespace
```

### dependencies

```bash
cd ~
ln -s ~/dotfiles/tmux ~/.config/
```
